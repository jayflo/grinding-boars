using System;
using System.Collections.Generic;

namespace LinkedIn
{
	public class Computer
	{
		public string[] RpnToPn(string[] rpn)
		{
			Stack<string> stack = new Stack();

			// push tokens on to stack
			// if token is an operator, pop previous two expressions
			// and prepend the token.  Strings on the stack can contain
			// more than one character

		}
	}
}